import { FC, ReactElement } from "react";
import { IPage } from "../../shared/models/Page";

const LoginPage: FC<IPage> = (): ReactElement => {

    return (<div>

    </div>);
}

export default LoginPage;